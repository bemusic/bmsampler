machine:
  services:
    - docker
dependencies:
  override:
    - docker info
    - docker build -t bmsampler .
    - mkdir dist
    - docker run bmsampler cat bmsampler.exe > dist/bmsampler.exe
    - cp vendor/libsndfile/lib/libsndfile-1.dll dist/
    - (cd dist && zip "$CIRCLE_ARTIFACTS/bmsampler" *)
test:
  override:
    - 'true'
